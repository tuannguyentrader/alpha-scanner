project: alpha-scanner
updated: 2026-03-21T00:00:00+08:00
repo: https://github.com/naimkatiman/alpha-scanner
phase: 8 # 1=frontend UI✅, 2=scanner engine✅, 3=multi-asset expansion✅, 4=broker API✅, 5=production hardening✅, 6=advanced analytics✅, 7=backtesting+charts+alerts✅, 8=prod hardening+AI+accounts✅

tasks:
  # Phase 1 — DONE
  - id: TASK-001
    status: done
    output: "Next.js 16 + Tailwind v4 + dark fintech scaffold"
  - id: TASK-002
    status: done
    output: "Dashboard layout - Navbar, sidebar, Footer"
  - id: TASK-003
    status: done
    output: "Symbol selector (XAUUSD, XAGUSD, BTCUSD, ETHUSD, XRPUSD)"
  - id: TASK-004
    status: done
    output: "Mode selector (Swing/Intraday/Scalper)"
  - id: TASK-005
    status: done
    output: "Risk profile selector (Conservative/Balanced/High Risk)"
  - id: TASK-006
    status: done
    output: "Signal panel with mock data"
  - id: TASK-007
    status: done
    output: "TP/SL visual display with Fibonacci levels"
  - id: TASK-008
    status: done
    output: "Settings panel (leverage, capital, TP ratio)"
  - id: TASK-009
    status: done
    output: "Mock signal data (15 configs: 5 symbols × 3 modes)"
  - id: TASK-010
    status: done
    output: "Mobile responsiveness + final polish. Commit f67e2a8."

  # Phase 2 — Scanner Engine — DONE
  - id: TASK-011
    status: done
    output: "Real-time price fetching with CoinGecko + metals APIs, 30s cache, usePrices hook, live status indicator. Commit fb68e70."
  - id: TASK-012
    status: done
    output: "S/R detection engine with pivot clustering algo, history API (CoinGecko OHLC + synthetic metals), /api/sr route, useSR hook (60s refresh), SRLevels visual component (price ladder with strength bars). Commit 97f940f."
  - id: TASK-013
    status: done
    output: "technicalAnalysis.ts (Wilder RSI, EMA, MACD), /api/indicators route (60s cache), useIndicators hook, IndicatorsPanel component (RSI gauge, MACD grid, EMA alignment). Commit 74bb054."
  - id: TASK-014
    status: done
    output: "Signal engine with weighted RSI/MACD/EMA/S&R scoring, /api/signals route (30s cache), useSignals hook, SignalPanel wired to live data with NEUTRAL support. Commit 7c18f63."
  - id: TASK-015
    status: done
    output: "TP/SL engine with ATR-based SL, S/R snapping, Fibonacci TPs, real position sizing. /api/tpsl route (30s cache), useTpSl hook, TpSlDisplay wired to live data. Commit b837cfe."

  # Phase 3 — Multi-Asset Expansion — DONE
  - id: TASK-016
    status: done
    output: "SymbolConfig type + SYMBOL_REGISTRY with 12 symbols (2 metals, 6 crypto, 4 forex)."
  - id: TASK-017
    status: done
    output: "prices/route.ts: fetchForexPrices() via fawazahmed0 USD rates, expanded CoinGecko for SOL/DOGE/ADA. history/route.ts: registry-driven lookups."
  - id: TASK-018
    status: done
    output: "All 5 API routes now use getAllSymbols() for VALID_SYMBOLS. tpslEngine.ts imports from symbols.ts."
  - id: TASK-019
    status: done
    output: "Registry-driven SymbolSelector with 3 independently collapsible categories."
  - id: TASK-020
    status: done
    output: "Clean TypeScript build. Commit 8339100: 'feat: Phase 3 - Multi-asset expansion'. Pushed to GitHub."

  # Phase 4 — Broker API Integration — DONE
  - id: TASK-021
    status: done
    owner: pm-agent
    notes: "MetaApi SDK + broker connection library"
    output: "metaapi.cloud-sdk installed. app/lib/brokerApi.ts with MetaApiWrapper, in-memory session store, connect/disconnect/getAccountInfo/getPositions. BrokerAccount/BrokerPosition types. serverExternalPackages configured in next.config.mjs."

  - id: TASK-022
    status: done
    owner: pm-agent
    notes: "Broker API routes"
    output: "4 API routes: POST /api/broker/connect (deploy+connect MT4/MT5), GET /api/broker/account (balance/equity/margin), GET /api/broker/positions (open positions+P&L), POST /api/broker/disconnect (cleanup)."

  - id: TASK-023
    status: done
    owner: pm-agent
    notes: "Broker connection UI"
    output: "BrokerConnect component in sidebar (collapsible, token+accountID form, connection status). AccountInfoCompact embedded (balance/equity/margin level/leverage). useBroker hook with localStorage session persistence, auto-reconnect on reload, 5s refresh."

  - id: TASK-024
    status: done
    owner: pm-agent
    notes: "Live positions panel"
    output: "PositionsPanel component (desktop table + mobile cards, total P&L summary). Purple dot indicator in SymbolSelector for symbols with open positions. Auto-refresh via useBroker (5s). Broker status indicator in header bar."

  - id: TASK-025
    status: done
    owner: pm-agent
    notes: "Signal alerts system"
    output: "alertEngine.ts (direction change detection, browser Notification API, watchlist/alerts localStorage). AlertsPanel component (watchlist tab with symbol toggles + history tab with alert rows). useAlerts hook (30s poll for watched symbols). AlertToast component (slide-in-right animation, 5s auto-dismiss). Desktop notification support with permission request."

  - id: TASK-026
    status: done
    owner: pm-agent
    notes: "Paper trading mode"
    output: "PaperTrading component ($10k virtual balance, quick BUY/SELL buttons, lot size presets, auto-trade toggle, open positions with live P&L, trade history with win rate/stats, reset account). usePaperTrading hook (localStorage persistence, auto-trade on signal changes, P&L calculation). Toggle switch to enable/disable."

  - id: TASK-027
    status: done
    owner: pm-agent
    notes: "Build verification + git commit"
    output: "Clean TypeScript build (0 errors). Commit a90225f: 'feat: Phase 4 - Broker API integration (MetaApi, live positions, signal alerts, paper trading)'. Pushed to GitHub. 20 files changed, 5048 insertions(+), 67 deletions(-)."

  # Phase 5 — Production Hardening + Mobile Polish
  - id: TASK-028
    status: done
    owner: coding-agent
    notes: "Error handling — ErrorBoundary component, loading skeletons for all panels, retry logic with exponential backoff in all hooks"
    output: "ErrorBoundary with retry button, per-panel error isolation, fetchWithRetry utility (exponential backoff + 429 handling), all 6 data hooks updated, loading skeletons, React.lazy/Suspense/memo, StaleIndicator, PWA manifest+SW, SEO (sitemap/robots/og), API rate limit middleware, input sanitization. Commit 2237584."

  - id: TASK-029
    status: done
    owner: coding-agent
    notes: "Rate limit handling — detect 429/rate limits from CoinGecko/fawazahmed0, serve cached data with stale indicator, backoff timer"
    output: "Rate limit detection in /api/prices (rateLimitedUntil flag, serves cached data with rateLimited+staleSince fields), StaleIndicator component shows CACHED badge, usePrices auto-slows to 120s interval when rate-limited, fetchWithRetry handles 429+Retry-After. Commit 2237584."

  - id: TASK-030
    status: done
    owner: coding-agent
    notes: "Mobile polish — audit all panels for responsive/touch-friendly on small screens (<640px). Fix overflow, touch targets, card layouts"
    output: "Safe-area padding for notched phones (env(safe-area-inset-*)), table overflow fix, PWA standalone mode tweaks, mobile-compact/mobile-card/mobile-stack CSS helpers, footer safe-area bottom, cleaned up duplicate CSS. All panels already have responsive grids (grid-cols-2 sm:grid-cols-4), flex-wrap, mobile cards vs desktop tables, 44px min touch targets. Commit d35dcb7+."

  - id: TASK-031
    status: done
    owner: coding-agent
    notes: "Performance — React.lazy + Suspense for heavy panels (IndicatorsPanel, PaperTrading, AlertsPanel), debounce rapid updates, React.memo where needed"
    output: "React.lazy for SRLevels, IndicatorsPanel, AlertsPanel, PaperTrading. Suspense with PanelSkeleton fallback. React.memo for SignalPanel, TpSlDisplay, PositionsPanel. usePrices dedup with inFlightRef. Commit 2237584."

  - id: TASK-032
    status: done
    owner: coding-agent
    notes: "PWA support — manifest.json, service worker, icons, install prompt, offline fallback page"
    output: "manifest.json (standalone, dark theme, 192+512 icons), sw.js (cache-first for static, network-first for API, offline fallback), offline.html, ServiceWorkerRegistrar component in layout. Commit 2237584."

  - id: TASK-033
    status: done
    owner: coding-agent
    notes: "SEO + metadata — og:title, og:description, og:image placeholder, favicon, per-route page titles, robots.txt, sitemap"
    output: "layout.tsx metadata (og:title, og:description, og:image, manifest link), sitemap.ts (dynamic generation), robots.txt, og-image.png placeholder, icon.svg favicon. Commit 2237584."

  - id: TASK-034
    status: done
    owner: coding-agent
    notes: "Security — sanitize all user inputs (broker form, settings), rate limit API routes (in-memory token bucket), CSP headers in next.config"
    output: "rateLimit.ts (token bucket: 60 req/min/IP with cleanup), apiGuard.ts (checkRateLimit helper with 429+Retry-After headers), sanitize.ts (input sanitization), all 10 API routes protected with checkRateLimit. Commit 2237584."

  - id: TASK-035
    status: done
    owner: coding-agent
    notes: "Build verification + git commit for Phase 5"
    output: "Clean TypeScript build (0 errors). All Phase 5 changes committed and pushed."

  # Phase 6 — Advanced Analytics + Social + Performance
  - id: TASK-036
    status: done
    owner: coding-agent
    notes: "Advanced Technical Indicators — Bollinger Bands (20,2), Stochastic Oscillator (14,3,3), integrate into IndicatorsPanel with visual gauges, add to signal engine as 2 new scoring factors (6 total)"
    output: "computeBollingerBands() + computeStochastic() in technicalAnalysis.ts, AllIndicators extended, signal engine now 6-factor weighted (RSI/MACD/EMA/SR 20% each, BB/Stoch 10% each), BollingerDisplay + StochasticDisplay components with %B gauge and %K/%D gauges, SignalPanel shows 6 tech badges. Clean build."

  - id: TASK-037
    status: done
    owner: coding-agent
    notes: "Signal History Timeline + Win Rate Tracker — localStorage persistence (last 500 signals), SignalHistory component with filterable timeline, auto-accuracy check (TP1 hit within 24h), win rate per symbol/mode/overall"
    output: "useSignalHistory hook (auto-record on direction change, TP1/SL accuracy checking against live price, 24h auto-expire, localStorage 500-record cap). SignalHistory component with timeline/stats tabs, filters (symbol/mode/outcome), win rate bars by symbol and mode. Clean build."

  - id: TASK-038
    status: done
    owner: coding-agent
    notes: "Multi-Timeframe Analysis Panel — /api/mtf route generating M15/H1/H4/D1 candles, compute indicators per timeframe, MultiTimeframe component showing 4-column direction+confidence view with confluence score"
    output: "/api/mtf route with candle resampling to M15/H1/H4/D1, computes full 6-factor signal per timeframe, confluence scoring (direction + %). useMTF hook (60s refresh). MultiTimeframe component with 4 cards (direction/confidence/RSI/EMA per TF) + confluence meter bar. Clean build."

  - id: TASK-039
    status: done
    owner: coding-agent
    notes: "Signal Sharing — Share button on SignalPanel, generate styled signal card (HTML→canvas→PNG), copy-to-clipboard text format, download PNG button"
    output: "ShareSignal component with modal (preview card + 3 actions). Copy-to-clipboard (formatted text with emoji), Download PNG (canvas-rendered 600x400 signal card at 2x DPR), Web Share API (native share on mobile). Integrated into SignalPanel header. Clean build."

  - id: TASK-040
    status: done
    owner: coding-agent
    notes: "Performance Analytics Dashboard — equity curve chart (canvas-based), daily equity snapshots in localStorage, drawdown visualization, profit by symbol/mode breakdown, key metrics (max drawdown%, profit factor, avg win/loss)"
    output: "usePerformanceAnalytics hook (daily equity snapshots, trade result recording, full metrics computation). PerformanceAnalytics component with canvas equity curve (gradient fill, drawdown shading, grid labels, DPR-aware), 8 key metric cards (P&L, drawdown, profit factor, peak equity, avg win/loss, streaks), breakdown tab (profit by symbol + mode with visual bars). Clean build."

  # Phase 7 — Backtesting, Charts, Leaderboard, Alerts, Telegram
  - id: TASK-041
    status: done
    owner: coding-agent
    notes: "Backtesting Engine — /api/backtest route, backtestEngine.ts, useBacktest hook, BacktestDashboard component, /backtest page"
    output: "backtestEngine.ts replays signals against historical OHLC (50-candle sliding window, ATR-based TP/SL, entry on direction change, exit on TP1/SL/reverse). /api/backtest route (5min cache, rate limited). BacktestDashboard with 8 metric cards, canvas equity curve, trades table. /backtest page with selectors. Clean build."

  - id: TASK-042
    status: done
    owner: coding-agent
    notes: "Price Chart with Signal Overlays — canvas candlestick chart with EMA/SR overlays"
    output: "PriceChart component: DPR-aware canvas, green/red candlesticks with wicks, EMA20 (yellow)/EMA50 (blue)/EMA200 (purple) lines, S/R dashed horizontals, signal markers (▲BUY/▼SELL), hover tooltip (O/H/L/C), last 50 candles. Integrated below SignalPanel. Clean build."

  - id: TASK-043
    status: done
    owner: coding-agent
    notes: "Signal Accuracy Leaderboard — /leaderboard page ranking symbol+mode combos"
    output: "Leaderboard component: reads signal history from localStorage, ranks by win rate (min 3 signals), 🥇🥈🥉 trophies, category filter tabs (All/Metals/Crypto/Forex), win rate bars, avg confidence, best streak. /leaderboard page. Clean build."

  - id: TASK-044
    status: done
    owner: coding-agent
    notes: "Custom Alert Rules — user-defined conditions with indicator evaluation"
    output: "alertRules.ts: 6 indicators (RSI/MACD_histogram/stochastic_k/bollinger_pctb/ema20_cross/price), 4 operators (</>/ crosses_above/crosses_below), AND/OR logic. AlertRuleBuilder component in sidebar (rule form, condition rows, enable/disable/edit/delete). useCustomAlerts hook (30s eval, 5min cooldown, browser notifications). Clean build."

  - id: TASK-045
    status: done
    owner: coding-agent
    notes: "Telegram Bot Integration — signal alerts via Telegram Bot API"
    output: "/api/telegram/send + /api/telegram/test routes. useTelegram hook (localStorage config, auto-send on direction change, HTML-formatted alerts with entry/TP/SL). TelegramSettings component in sidebar (token/chatID inputs, show/hide toggle, test connection, auto-send toggle, BotFather instructions). Clean build."

  # Phase 8 — Production Hardening + AI Signals + User Accounts
  - id: TASK-046
    status: done
    owner: pm-agent
    notes: "User accounts + saved settings — NextAuth.js (Telegram OAuth provider). UserSettings Prisma model: saved symbol watchlist, preferred mode, risk profile, leverage/capital, Telegram bot config. Persist all settings to DB. Load on login. Guest mode still works without login."
    output: "NextAuth v4 credentials provider (email+password), Prisma 5 + SQLite (User + UserSettings models), /api/auth/register + /api/settings (GET/PUT), AuthProvider SessionProvider wrapper, UserMenu component (initials avatar + dropdown), login/register pages at /auth/signin + /auth/register, useUserSettings hook (DB for logged-in, localStorage fallback for guests), bcryptjs password hashing. Commit a7d19f9."

  - id: TASK-047
    status: done
    owner: pm-agent
    notes: "AI signal commentary — GPT-4o-mini integration. After signal generation, call OpenAI to write a 2-sentence plain-English explanation of WHY the signal fired (which indicators aligned, what the key confluence was). Display in SignalPanel below the main signal card. Fallback gracefully if API unavailable."
    output: "aiCommentary.ts (GPT-4o-mini with 8s timeout, 2min cache, rule-based fallback if no API key or error). /api/commentary POST route. useCommentary hook (deduped by direction+confidence). SignalCommentary component (sparkles icon for AI, message icon for fallback). Integrated below SignalPanel. Commit 98ee353."

  - id: TASK-048
    status: done
    owner: pm-agent
    notes: "Signal accuracy tracker v2 — server-side accuracy tracking via PostgreSQL (not just localStorage). SignalRecord model: symbol, mode, direction, entryPrice, tp1, sl, timestamp, outcome (HIT_TP1/HIT_SL/EXPIRED/PENDING). Cron job checks open signals every 15min against live price. Public accuracy stats at /accuracy page per symbol."
    output: "SignalRecord Prisma model (symbol, mode, direction, entryPrice, tp1, sl, confidence, outcome, timestamps). Auto-record on signal generation (fire-and-forget). /api/accuracy/check GET (cron endpoint, checks pending signals vs live prices, resolves HIT_TP1/HIT_SL/EXPIRED after 24h). /api/accuracy GET (stats by symbol + overall + recent records). /api/signals/record POST (manual recording). /accuracy page with overall stats cards, per-symbol win rate bars, recent signals table. Navbar link added. Commit bdd0693."

  - id: TASK-049
    status: done
    owner: pm-agent
    notes: "Webhook outbound — allow users to configure a custom webhook URL. On every new signal, POST a JSON payload (symbol, direction, entry, TP/SL, confidence, timestamp) to their URL. Retry 3x with backoff. Admin dashboard to see webhook delivery logs. Useful for connecting to trading bots."
    output: "WebhookConfig + WebhookLog Prisma models. webhookDispatcher.ts (3x retry with exponential backoff, 10s timeout). Auto-dispatch on new signal via signals route. /api/webhook/configure (GET/POST/PUT/DELETE). /api/webhook/logs GET. WebhookSettings sidebar component (add URL, enable/disable toggle, delete, inline delivery logs). Commit a81c5c5."

  - id: TASK-050
    status: done
    owner: pm-agent
    notes: "taste-skill frontend redesign — install taste-skill (npx skills add https://github.com/Leonxlnx/taste-skill --yes), read .agents/skills/redesign-existing-projects/SKILL.md and .agents/skills/high-end-visual-design/SKILL.md, apply Ethereal Glass vibe (OLED dark #050505, emerald #10B981 accent). Redesign: globals.css, layout, Navbar (floating glass pill), dashboard cards, signal panel, chart, leaderboard, backtest page. framer-motion animations. Build must pass."
    output: "Full Ethereal Glass redesign. globals.css: OLED #050505 base, glass-panel/glass-card with blur+saturate+emerald inner glow, floating-navbar class (rounded-full glass pill), ambient-glow animation (radial emerald), text-gradient-emerald, refined scrollbars+shadows. Navbar: floating pill with rounded-full links, animated Zap logo, emerald LIVE badge. Layout: #050505 body. All 14 panel components: bg-[#0a0a0a] cards. Auth/accuracy/backtest/leaderboard pages: #050505 backgrounds. framer-motion on navbar logo+hamburger. Commit 609430c."

  # Phase 9 — Social + Community + Deployment
  - id: TASK-051
    status: done
    owner: pm-agent
    notes: "Signal leaderboard — public /leaderboard page ranking users by signal accuracy (server-side, from SignalRecord DB). Show top 20 traders, win rate %, total signals, streak. Weekly/monthly/all-time tabs. No auth required. OG metadata for shareability."
    output: "/api/leaderboard GET route (SignalRecord groupBy symbol+mode, win rate/streak calc, period filter weekly/monthly/all, top 20 min 3 signals, rate limited). useLeaderboard hook (60s auto-refresh). Leaderboard component (3 tab buttons, summary cards, trophy-ranked table with emerald win rate bars, loading skeleton). LeaderboardClient.tsx split for server metadata. OG metadata via generateMetadata. Commit 680009b."

  - id: TASK-052
    status: done
    owner: pm-agent
    notes: "One-click deploy — add Deploy to Vercel + Deploy to Railway buttons to README. Create vercel.json + railway.json with correct env var prompts. Document all required env vars in .env.example. Deployment guide in docs/DEPLOY.md."
    output: "vercel.json (prisma generate + build), railway.toml (build+deploy config), .env.example (6 env vars documented), docs/DEPLOY.md (full guide: Vercel/Railway step-by-step, env table, DB notes, troubleshooting), README.md with deploy buttons. Commit 93d054f."

  - id: TASK-053
    status: done
    owner: pm-agent
    notes: "Signal feed — public /feed page showing live signal stream across all symbols, sorted by recency. Each card: symbol, direction badge, confidence %, timeframe, timestamp. No auth. Auto-refresh every 30s. RSS feed at /feed.xml for external subscribers."
    output: "/api/feed GET (last 50 signals, symbol/limit filters, rate limited). /feed page with FeedClient (30s auto-refresh, symbol filter dropdown, glass signal cards with direction/confidence/entry/TP/SL/timestamp). /feed.xml RSS 2.0 route. Navbar Feed link with RSS icon. Commit ffa2ab3."

  - id: TASK-054
    status: done
    owner: pm-agent
    notes: "Strategy templates — let users save/load signal configurations as named strategies (e.g. 'Conservative Gold', 'Aggressive BTC Scalper'). StrategyTemplate Prisma model (userId, name, config JSON). CRUD API. Strategy picker dropdown in sidebar. Share strategy via public link."
    output: "StrategyTemplate Prisma model (userId, name, description, config JSON, isPublic, shareSlug@unique). /api/strategies GET+POST, /api/strategies/[id] GET+PUT+DELETE. StrategyPicker sidebar component (save current config, list/apply/delete/share strategies, guest localStorage fallback max 5). /strategy/[slug] public share page with OG metadata and 'Use This Strategy' CTA. URL param hydration on homepage (?symbol=&mode=&risk=&leverage=&capital=). Clean build."

  - id: TASK-055
    status: done
    owner: pm-agent
    notes: "CI/CD pipeline — GitHub Actions workflow: lint + tsc + build on every PR, auto-deploy to Railway on merge to main, Lighthouse CI score check (performance > 80, accessibility > 90). Badge in README. Telegram notification on deploy success/failure."
    output: "GitHub Actions ci.yml (build job: Node 22 + npm ci + cache + tsc --noEmit + npm run build; lighthouse job: treosh/lighthouse-ci-action@v12 on PRs; deploy job: Railway CLI on push to main; notify job: Telegram curl with status/SHA/commit message). lighthouserc.json (perf>=0.80 warn, a11y>=0.90 error). README badge. Needs 3 secrets: RAILWAY_TOKEN, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID. Commit f340bcc."

  # Phase 10 — Open Source Launch + Monetisation
  - id: TASK-056
    status: done
    owner: pm-agent
    notes: "GitHub repo polish for open source launch — clean README with hero banner (badges: stars, forks, license, CI status), feature list with screenshots/GIFs, quick-start guide (Docker + manual), architecture diagram (Mermaid), contributing guide (CONTRIBUTING.md), issue templates (bug, feature request), PR template. Make it star-worthy."
    output: "README with hero banner, 5 badges (CI/license/stars/forks/social), 17-feature table, Mermaid architecture diagram, tech stack table, quick-start (manual+Docker), env vars table, deploy buttons, star history chart. CONTRIBUTING.md (fork/branch/PR workflow, code standards, commit convention). Issue templates (bug_report.md, feature_request.md). PR template. MIT LICENSE. Commit d131f64."

  - id: TASK-057
    status: done
    owner: pm-agent
    notes: "Docker support — Dockerfile (multi-stage: builder + runner), docker-compose.yml (app + postgres), .dockerignore, health check endpoint at /api/health (returns version, uptime, DB status). Document Docker setup in README and docs/DEPLOY.md. Image should be < 200MB."
    output: "3-stage Dockerfile (deps→builder→runner, node:22-alpine, non-root nextjs user, standalone output). docker-compose.yml (app+postgres:16-alpine, healthcheck, volume). .dockerignore. /api/health route (status/version/uptime/timestamp/database connectivity). next.config output:'standalone'. README+DEPLOY.md updated. Commit 5af8610."

  - id: TASK-058
    status: done
    owner: pm-agent
    notes: "API key monetisation layer — ApiKey Prisma model (userId, key, plan: free/pro, rateLimit, expiresAt). Free plan: 100 req/day. Pro plan: unlimited. Middleware to validate API keys on /api/signals, /api/prices, /api/indicators. Dashboard page /api-keys for generating/revoking keys. Foundation for future paid tier."
    output: "ApiKey Prisma model (cuid id, userId FK cascade, key@unique as_+nanoid32, name, plan free/pro, requestsToday, lastResetAt, expiresAt?, enabled, createdAt, @@index userId+key). prisma db push applied. app/lib/apiKeyAuth.ts (validateApiKey: Bearer header or ?api_key param, checks exists/enabled/not-expired, auto-reset daily counter UTC, rate limit 100 free/10000 pro, increments requestsToday). /api/api-keys GET (list masked) + POST (create, full key shown once). /api/api-keys/[id] PUT (name/enabled) + DELETE. Optional API key auth added to /api/signals, /api/prices, /api/indicators (additive — public still works without key). /api-keys page with glass card design (list keys, usage bar, create modal, full key copy-once, delete confirm). Navbar API Keys link. nanoid installed. Clean build."

  - id: TASK-059
    status: pending
    owner: pm-agent
    notes: "Demo mode + onboarding — /demo route with pre-loaded mock signals (no API keys needed), guided tour overlay (custom lightweight), 5-step onboarding checklist (view signal, set risk profile, connect broker, enable alerts, share signal). Persist tour completion in localStorage. Show 'Try Demo' CTA on landing page."

  - id: TASK-060
    status: pending
    owner: pm-agent
    notes: "Landing page — dedicated marketing landing page at root / (separate from /dashboard). Hero: headline + animated signal card demo. Features grid: 6 feature cards with icons. Social proof: signal accuracy stats from DB. Pricing section: Free vs Pro tiers. Footer with links. SEO optimised. Converts visitors to signups."

next_actions:
  - "Phase 10 defined: 5 tasks (TASK-056 to TASK-060). Start with TASK-056."

approval_pending: false
pending_commit_summary: null
